language: python
install:
- pip install -r requirements.txt
script:
  - pip install -r requirements.txt
  - mkdocs build --config-file=docs/mkdocs/mkdocs.yml --verbose --clean
  - mv docs/mkdocs/site .
  - mkdir solstice_repos
  - cd solstice_repos
  - git clone https://github.com/Solstice-Short-Film/solstice.git
  - cd ..
  - sphinx-apidoc -f -e -o docs/sphinx solstice_repos/solstice/solstice --tocfile solstice-modules
  - sphinx-build -M html ./docs/sphinx ./
  - cd html && touch .nojekyll && cd ../
  - cd site && touch .nojekyll && cd ../
  - mv site pipeline
  - mv pipeline html
deploy:
  - provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    local_dir: html
    on:
      branch: master
